ML TODO

Keep this file short.
Use repo-root ROADMAP.MD for the bigger plan.

1. Keep the unified model path clean and understandable.
2. Delete or quarantine old pretrain/sequence branches once the unified path is stable enough.
3. Keep pre-match safety strict: no future info, no post-game-only labels disguised as inputs.
4. Add train-split-only normalization stats for unified continuous inputs instead of relying only on bounded scaling.
5. Rework evaluation to be trustworthy for the real target:
temporal validation
multi-seed comparisons
calibration-aware metrics
6. Tune the unified model first:
dropout
batch size ceiling
hidden width
7. Replace the in-memory SQLite training build with streamed Parquet shards and an IterableDataset once data volume grows further.
8. Defer rank and other external player metadata until we have a production-safe and non-leaky plan.
